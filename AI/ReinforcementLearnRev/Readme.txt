To compile, simply use javac *.java.  

The program requires a filename be passed via command line.

It will output the utility of every 100th state chosen during reinforcement learning.  It also outputs the final utilities and the policy.  